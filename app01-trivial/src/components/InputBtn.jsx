export default function InputBtn({text, name}){

    return (

        <div>
            <p className="comp">Component InputBtn</p>
        
            <button>{text} {name}</button>
        </div>

    );
}